#!/bin/bash

# omarchy:summary=Restart an application by killing it and relaunching it.
# omarchy:args=<application-name> [application-args...]

pkill -x $1
setsid omartix-app -- "$@" >/dev/null 2>&1 &
