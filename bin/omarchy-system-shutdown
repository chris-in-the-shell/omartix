#!/bin/bash

# omarchy:summary=Shut down after closing application windows
# omarchy:examples=omarchy shutdown | omarchy system shutdown
# omarchy:aliases=omarchy shutdown

# Schedule the shutdown outside the terminal so closing windows does not
# terminate it first. On Omartix, elogind owns session power actions.
nohup /usr/bin/sh -c 'sleep 2; exec /usr/bin/loginctl poweroff' >/dev/null 2>&1 &

omarchy-osd -i shutdown -m "Shutting down" -d 5000

omarchy-state clear re*-required

# Now close all windows
omarchy-hyprland-window-close-all
sleep 1 # Allow apps like Chrome to shutdown correctly
