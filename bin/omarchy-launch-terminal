#!/bin/bash

# omarchy:summary=Launch a terminal in the active terminal's current directory
# omarchy:args=[command...]

exec setsid omartix-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" "$@"
