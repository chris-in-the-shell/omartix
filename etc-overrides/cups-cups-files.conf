# Omartix override applied after Artix's cups package owns its configuration.
# Keep CUPS administration with system administrators and use peer credentials
# for local authorization.
SystemGroup sys root
PeerCred on
